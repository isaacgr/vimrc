# Isaac's Vim Configuration

This repository contains my vimrc file.

To use my Vim configuration in Linux, clone this repository and:

    mv ~/.vimrc ~/.vimrc.old
    mv ~/.vim ~/.vim.old
    cp vimrc ~/.vimrc
    cp coc-settings.json ~/.vim

Open Vim and run:

    :PlugInstall
